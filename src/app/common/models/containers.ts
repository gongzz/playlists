export interface Container {
  id: string; // Unique identifier for the container
  name: string; // Name of the container
  description: string; // A brief description of the container
  userId: string;
}
